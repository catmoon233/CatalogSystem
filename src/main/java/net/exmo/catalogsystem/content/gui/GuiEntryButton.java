package net.exmo.catalogsystem.content.gui;

import se.mickelus.mutil.gui.GuiButton;
import se.mickelus.mutil.gui.GuiElement;
import se.mickelus.mutil.gui.GuiRect;

public class GuiEntryButton {

    public GuiEntryButton(int x, int y, int width, int height, String text, Runnable onClick) {
    }

    public GuiButton GuiEntryPaddingButton(int imageWidth, int imageHeight, String text, Runnable onClick) {
        GuiButton button = new GuiButton(x, y, width, height, text, onClick)
        return ;
    }
}
